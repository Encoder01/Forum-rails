<%  content_for(:forum_content) do %>
<div class="large-12 columns">
  <h1>Hoşgeldiniz</h1>
  <div class="topics panel columns radius">
    <% if @topics.any? %>
        <%# if ile konu varmı kontrol ettik var ise tüm konu başlıkları görüntüledik%>
    <%= render @topics%>
        <%else%>
    <p>Henüz konu açılmamış.</p>
        <%end%>
  </div>
</div>
<% end%>